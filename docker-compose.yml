services:
  web:
    build:
      context: .
      args:
        VITE_MSAL_CLIENT_ID: ${VITE_MSAL_CLIENT_ID:-}
        VITE_MSAL_AUTHORITY: ${VITE_MSAL_AUTHORITY:-}
        VITE_MSAL_AUTHORITY_SIGNUP: ${VITE_MSAL_AUTHORITY_SIGNUP:-}
    ports:
      - "8081:80"
    restart: unless-stopped
